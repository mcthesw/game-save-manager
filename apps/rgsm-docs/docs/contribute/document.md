---
id: document
---

# 改进文档

点击页面底部的 **编辑此页** 即可修改。用户文档位于主仓库的 `apps/rgsm-docs`，与软件一起维护。

按当前界面，用简短步骤和截图说明操作。截图说明省略句末标点。

中文页面位于 `docs/`，英文页面在 `i18n/en/docusaurus-plugin-content-docs/current/` 中保持相同结构。修改指南时同步更新两种语言。
